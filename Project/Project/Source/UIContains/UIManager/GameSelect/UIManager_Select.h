#pragma once

#include "../Base/UIManager.h"

class UIManager_Select : public UIManager {
public:
	UIManager_Select(const IWorldPtr& world, const IGameManagerPtr& gameManager);

private:
	//virtual void regist() override;
	//virtual void create() override;
};